aws_region        = "us-east-1"
state_bucket_name = "existing-versioned-terraform-state-bucket"
github_repository = "OCHOLA-EDDYPHIL/hindsight"
lifecycle_database_url_parameter_name = "/hindsight/demo/lifecycle-database-url"
github_subjects = [
  "repo:OCHOLA-EDDYPHIL/hindsight:environment:demo",
]
domain_name        = "hindsight.strathmoreedu.qzz.io"
cloudflare_zone_id = "3545d878227ee71fb3df8d4b50d3d193"

# Optional locked cross-region lifecycle export replica:
# enable_cold_region_recovery_profile = true
# cold_region_recovery_region         = "us-west-2"

# If the account already has GitHub's OIDC provider:
# create_github_oidc_provider       = false
# existing_github_oidc_provider_arn = "arn:aws:iam::123456789012:oidc-provider/token.actions.githubusercontent.com"
